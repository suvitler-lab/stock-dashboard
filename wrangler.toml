name = "stock-dashboard"
main = "worker.js"
compatibility_date = "2024-11-01"

# Worker + Static Assets — เสิร์ฟไฟล์ใน ./public ผ่าน binding ASSETS
# Worker รันเฉพาะ route ที่ไม่ใช่ไฟล์ static (เช่น /api/ics = proxy ปฏิทิน iCal)
[assets]
directory = "./public"
binding = "ASSETS"
